#include "main.h"
/**
 * puts2 - function
 * @str: first parameter
 * Return: 0 (Success);
 */
void puts2(char *str)
{
	int len=0;

	for ( ;s[len] != '\0' ; len = len + 2 /* len +=2 */ )
	{
		_putchar(s[len]);
	}
}
