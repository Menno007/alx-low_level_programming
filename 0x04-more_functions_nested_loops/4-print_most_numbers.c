#include "main.h"
/**
 * print_numbers - blablabla
 *
 * Description: blablabla
 *
 * Return: blablabla
 */
void print_most_numbers(void)
{
	int x;

	for (x = 48; x <= 57; x++)
	{
		if ((x != 52) && (x != 50))
		_putchar(x);
	}
	_putchar('\n');
}
